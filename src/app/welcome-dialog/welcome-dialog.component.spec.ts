import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { MaterialModule, MdDialogRef, OverlayRef, MdDialogContainer } from "@angular/material";
import { WelcomeDialogComponent } from './welcome-dialog.component';

// TODO: Fix test
/*let mockDialogRef = new MdDialogRef(new OverlayRef(null, null, null, null), new MdDialogContainer(null, null, null, null));

describe('WelcomeDialogComponent', () => {
  let component: WelcomeDialogComponent;
  let fixture: ComponentFixture<WelcomeDialogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [MaterialModule],
      declarations: [WelcomeDialogComponent],
      providers: [{ provide: MdDialogRef, useInstance: mockDialogRef }]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WelcomeDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
    
    mockDialogRef.componentInstance = component;
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});*/
